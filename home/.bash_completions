#!/usr/bin/env bash

complete -W "append create done edit finished lc ld lp ls priority report today usage" todo

complete -W "gnomebatfix hublan_fix morning pw qr scan sync" lazyscript

complete -W "all bitrot prep restic" backup

complete -W "email" agenda
